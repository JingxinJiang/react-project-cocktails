import React from 'react'

const About = () => {
  return (
    <section className='about-section section' >
      <h2 className='section-title'>About us</h2>
      <p>components' names must start with capital letter , because , when you write component name with small letter , compiler runs it like as html tag, but when you write with capital letter , compiler runs it from your javascript file location.</p>
    </section>
  )
}

export default About
